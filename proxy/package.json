{
  "name": "buzzword-decoder-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Local proxy that keeps the Anthropic API key off the Buzzword Decoder Chrome extension.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
